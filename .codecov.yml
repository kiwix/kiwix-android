codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        threshold: 0.1%
    patch:
      default:
        target: 100%
        threshold: 20%

ignore:
  - "docs"
  - "fastlane"
  - "core/src/test/java/"
